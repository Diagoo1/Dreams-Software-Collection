<Window x:Class="Dreams.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{DynamicResource AppSettings}" 
        Height="555" Width="450"  
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        FlowDirection="{DynamicResource ApplicationFlowDirection}">

    <Window.Resources>
        <!-- GRADIENTS -->
        <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0EA5E9" Offset="0"/>
            <GradientStop Color="#0284C7" Offset="1"/>
        </LinearGradientBrush>

        <!-- STYLES - CLOSE BUTTON -->
        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource DynamicError}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8">
                            <TextBlock Text="{TemplateBinding Content}" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="13" Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#1AEF4444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- STYLES - BUG REPORT BUTTON -->
        <Style x:Key="BugReportButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource DynamicAccent}"/>
            <Setter Property="ToolTip" Value="{DynamicResource ReportBug}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8">
                            <TextBlock Text="{TemplateBinding Content}" 
                               FontFamily="Segoe MDL2 Assets" 
                               VerticalAlignment="Center" 
                               HorizontalAlignment="Center" 
                               FontSize="14" 
                               Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#1A0EA5E9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- STYLES - MODERN BUTTON (Like About Page) -->
        <Style x:Key="ModernBtn" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentGradient}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- STYLES - MODERN SWITCH -->
        <Style x:Key="ModernSwitch" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Cursor="Hand">
                            <Border x:Name="Background" Width="38" Height="20" CornerRadius="10" Background="{DynamicResource DynamicBorderBrush}">
                                <Ellipse x:Name="Dot" Width="14" Height="14" Fill="White" HorizontalAlignment="Left" Margin="3,0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform X="0"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Background" Property="Background" Value="{DynamicResource DynamicAccent}"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Dot" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="18" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Dot" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- STYLES - MODERN SLIDER -->
        <Style x:Key="ModernSlider" TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border Height="4" Background="{DynamicResource DynamicBorderBrush}" CornerRadius="2" VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Width="14" Height="14" Background="White" BorderBrush="{DynamicResource DynamicAccent}" BorderThickness="2" CornerRadius="7"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ✅ STYLES - ORIGINAL MODERN COMBOBOX -->
        <Style x:Key="ModernCombo" TargetType="ComboBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Foreground" Value="{DynamicResource DynamicMainText}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press" Focusable="False">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border" Background="{DynamicResource DynamicCardBg}" BorderBrush="{DynamicResource DynamicBorderBrush}" BorderThickness="1" CornerRadius="8"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Grid Margin="12,0" IsHitTestVisible="False">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                <Path x:Name="Arrow" Grid.Column="1" Data="M 0 0 L 4 4 L 8 0" Stroke="{DynamicResource DynamicSubText}" StrokeThickness="2" VerticalAlignment="Center" Margin="5,1,0,0" RenderTransformOrigin="0.5,0.5">
                                    <Path.RenderTransform>
                                        <RotateTransform Angle="0"/>
                                    </Path.RenderTransform>
                                </Path>
                            </Grid>
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" Placement="Bottom" PopupAnimation="Fade">
                                <Grid MinWidth="{TemplateBinding ActualWidth}" MaxHeight="250">
                                    <Border x:Name="DropDownBorder" Background="{DynamicResource DynamicCardBg}" CornerRadius="10" BorderThickness="1" BorderBrush="{DynamicResource DynamicBorderBrush}" Margin="0,5,0,10" Opacity="0" RenderTransformOrigin="0.5,0">
                                        <Border.RenderTransform>
                                            <TranslateTransform Y="-10"/>
                                        </Border.RenderTransform>
                                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                            <StackPanel IsItemsHost="True" Margin="2"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Arrow" Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" To="180" Duration="0:0:0.25"/>
                                            <DoubleAnimation Storyboard.TargetName="DropDownBorder" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="DropDownBorder" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.25"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Arrow" Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" To="0" Duration="0:0:0.25"/>
                                            <DoubleAnimation Storyboard.TargetName="DropDownBorder" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="DropDownBorder" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="-10" Duration="0:0:0.25"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ✅ STYLES - ORIGINAL COMBOBOX ITEM -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Foreground" Value="{DynamicResource DynamicMainText}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="itemBorder" CornerRadius="6" Margin="2" Background="Transparent">
                            <ContentPresenter Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="itemBorder" Property="Background" Value="#100EA5E9"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="itemBorder" Property="Background" Value="{DynamicResource DynamicAccent}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource DynamicWindowBg}" CornerRadius="20" BorderBrush="{DynamicResource DynamicBorderBrush}" BorderThickness="1.5" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Grid Grid.Row="0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                    <Border Width="10" Height="10" CornerRadius="5" Background="#FF5F57" Margin="0,0,6,0"/>
                    <Border Width="10" Height="10" CornerRadius="5" Background="#FEBC2E" Margin="0,0,6,0"/>
                    <Border Width="10" Height="10" CornerRadius="5" Background="#28C840" Margin="0,0,12,0"/>
                    <TextBlock Text="{DynamicResource AppSettings}" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource DynamicMainText}"/>
                </StackPanel>
                <!-- ✅ Buttons Panel (right side) -->
                <StackPanel Orientation="Horizontal" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Center" 
                Margin="0,0,10,0">
                    <Button x:Name="btnReportBug" 
                Content="&#xEBE8;" 
                Style="{StaticResource BugReportButton}" 
                Margin="0,0,4,0"
                Click="btnReportBug_Click"/>
                    <Button x:Name="btnClose" 
                Content="&#xE8BB;" 
                Style="{StaticResource CloseButton}" 
                Click="btnClose_Click"/>
                </StackPanel>
            </Grid>

            <!-- CONTENT -->
            <StackPanel Grid.Row="1" Margin="18,0,18,10">

                <!-- GENERAL -->
                <TextBlock Text="{DynamicResource General}" Margin="5,8,5,4" Foreground="{DynamicResource DynamicAccent}" FontWeight="Bold" FontSize="11"/>
                <Border Background="{DynamicResource DynamicCardBg}" CornerRadius="12" Padding="12,10" Margin="0,0,0,8" BorderBrush="{DynamicResource DynamicBorderBrush}" BorderThickness="1">
                    <Grid>
                        <StackPanel Orientation="Horizontal">
                            <Border Width="32" Height="32" Background="#100EA5E9" CornerRadius="8" Margin="0,0,10,0">
                                <TextBlock Text="&#xE134;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource DynamicAccent}" FontSize="13"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{DynamicResource Language}" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource DynamicMainText}"/>
                                <TextBlock Text="{DynamicResource ChoosePreferredInterface}" FontSize="9" Foreground="{DynamicResource DynamicSubText}"/>
                            </StackPanel>
                        </StackPanel>
                        <ComboBox x:Name="cmbLanguage" Style="{StaticResource ModernCombo}" HorizontalAlignment="Right" Width="125" VerticalAlignment="Center">
                            <ComboBoxItem Content="{DynamicResource EnglishUS}"/>
                            <ComboBoxItem Content="{DynamicResource Arabic}"/>
                            <ComboBoxItem Content="{DynamicResource French}"/>
                            <ComboBoxItem Content="{DynamicResource Spanish}"/>
                            <ComboBoxItem Content="{DynamicResource Russian}"/>
                        </ComboBox>
                    </Grid>
                </Border>

                <!-- APPEARANCE -->
                <TextBlock Text="{DynamicResource Appearance}" Margin="5,8,5,4" Foreground="{DynamicResource DynamicAccent}" FontWeight="Bold" FontSize="11"/>
                <Border Background="{DynamicResource DynamicCardBg}" CornerRadius="12" Padding="12,10" Margin="0,0,0,8" BorderBrush="{DynamicResource DynamicBorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="32" Height="32" Background="#10EC4899" CornerRadius="8" Margin="0,0,10,0">
                                    <TextBlock Text="&#xE2B1;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource DynamicPink}" FontSize="13"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{DynamicResource DarkMode}" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource DynamicMainText}"/>
                                    <TextBlock Text="{DynamicResource GlobalTheme}" FontSize="9" Foreground="{DynamicResource DynamicSubText}"/>
                                </StackPanel>
                            </StackPanel>
                            <CheckBox x:Name="chkDarkMode" Style="{StaticResource ModernSwitch}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="ToggleTheme"/>
                        </Grid>
                        <Border Height="1" Background="{DynamicResource DynamicBorderBrush}" Margin="42,0,0,12" Opacity="0.5"/>
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <Border Width="32" Height="32" Background="#10F97316" CornerRadius="8" Margin="0,0,10,0">
                                    <TextBlock Text="&#xE890;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource DynamicWarning}" FontSize="13"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{DynamicResource WindowOpacity}" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource DynamicMainText}" VerticalAlignment="Center"/>
                                        <Border Background="#300EA5E9" Padding="8,3" CornerRadius="10" Margin="10,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding ElementName=sldOpacity, Path=Value, StringFormat={}{0:F0}%}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource DynamicAccent}"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{DynamicResource AdjustTransparency}" FontSize="9" Foreground="{DynamicResource DynamicSubText}"/>
                                </StackPanel>
                            </StackPanel>
                            <Slider x:Name="sldOpacity" Style="{StaticResource ModernSlider}" Width="120" Minimum="50" Maximum="100" Value="100" VerticalAlignment="Center" HorizontalAlignment="Right" ValueChanged="sldOpacity_ValueChanged" IsSnapToTickEnabled="True" TickFrequency="5"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- SYSTEM -->
                <TextBlock Text="{DynamicResource System}" Margin="5,8,5,4" Foreground="{DynamicResource DynamicAccent}" FontWeight="Bold" FontSize="11"/>
                <Border Background="{DynamicResource DynamicCardBg}" CornerRadius="12" Padding="12,10" BorderBrush="{DynamicResource DynamicBorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="32" Height="32" Background="#1022C55E" CornerRadius="8" Margin="0,0,10,0">
                                    <TextBlock Text="&#xE37A;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource DynamicSuccess}" FontSize="13"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{DynamicResource RunAtStartup}" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource DynamicMainText}"/>
                                    <TextBlock Text="{DynamicResource LaunchAutomatically}" FontSize="9" Foreground="{DynamicResource DynamicSubText}"/>
                                </StackPanel>
                            </StackPanel>
                            <CheckBox x:Name="chkStartup" Style="{StaticResource ModernSwitch}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                        <Border Height="1" Background="{DynamicResource DynamicBorderBrush}" Margin="42,0,0,10" Opacity="0.5"/>
                        <Grid Margin="0,0,0,10">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="32" Height="32" Background="#108B5CF6" CornerRadius="8" Margin="0,0,10,0">
                                    <TextBlock Text="&#xE743;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource DynamicPurple}" FontSize="13"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{DynamicResource MinimizeToTray}" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource DynamicMainText}"/>
                                    <TextBlock Text="{DynamicResource CloseToTrayDescription}" FontSize="9" Foreground="{DynamicResource DynamicSubText}"/>
                                </StackPanel>
                            </StackPanel>
                            <CheckBox x:Name="chkTray" Style="{StaticResource ModernSwitch}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                        <Border Height="1" Background="{DynamicResource DynamicBorderBrush}" Margin="42,0,0,10" Opacity="0.5"/>
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <Border Width="32" Height="32" Background="#10EF4444" CornerRadius="8" Margin="0,0,10,0">
                                    <TextBlock Text="&#xE9D9;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource DynamicError}" FontSize="13"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{DynamicResource HardwareAcceleration}" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource DynamicMainText}"/>
                                    <TextBlock Text="{DynamicResource UseGPU}" FontSize="9" Foreground="{DynamicResource DynamicSubText}"/>
                                </StackPanel>
                            </StackPanel>
                            <CheckBox x:Name="chkHardware" Style="{StaticResource ModernSwitch}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="chkHardware_Click"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- FOOTER -->
            <Border Grid.Row="2" Background="{DynamicResource DynamicCardBg}" CornerRadius="0,0,20,20" Padding="20,12" BorderBrush="{DynamicResource DynamicBorderBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource DreamsSoftwareVersion}" FontSize="9" FontWeight="SemiBold" Foreground="{DynamicResource DynamicMainText}"/>
                        <TextBlock Text="{DynamicResource ChangesSavedOnClose}" FontSize="8" Foreground="{DynamicResource DynamicSubText}"/>
                    </StackPanel>
                    <Button x:Name="btnSave" Content="{DynamicResource ApplyChanges}" HorizontalAlignment="Right" Style="{StaticResource ModernBtn}" Width="120" Click="btnSave_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>